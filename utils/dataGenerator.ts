
import { Student, ShiftRequirement } from '../types';

export const generateMockStudents = (): Student[] => {
  // Returning empty array as user requested to input data themselves
  return [];
};

export const generateMockShifts = (): ShiftRequirement[] => {
  // Returning empty array as user requested to input data themselves
  return [];
};
